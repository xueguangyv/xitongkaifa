1.
mkdir -p q09/
cd q09
touch src/greetlab/__init__.py
cat > src/greetlab/cli.py <<'EOF'
import argparse
def main():
    p = argparse.ArgumentParser()
    p.add_argument("--name", required=True)
    a = p.parse_args()
    print(f"Hello, {a.name}!")
EOF
cat > pyproject.toml <<'EOF'
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "greetlab-25020007149"
version = "0.1.0"
[project.scripts]
sdt-greet = "greetlab.cli:main"
EOF
sudo apt install python3.13-venv
python3 -m venv .vene
2.
cp -r q09 q10
cd q10
cat > test_greetlab.py <<'EOF'
import pytest
from greetlab.cli import main
import sys

def test_normal_name(monkeypatch, capsys):
    monkeypatch.setattr(sys, 'argv', ["", "--name", "Alice"])
    main()
    captured = capsys.readouterr()
    assert captured.out.strip() == "Hello, Alice!"

def test_blank_name_exit(monkeypatch):
    # name只有空白字符（空格），预期抛出SystemExit，状态码=2
    monkeypatch.setattr(sys, 'argv', ["", "--name", "   "])
    with pytest.raises(SystemExit) as excinfo:
        main()
    assert excinfo.value.code == 2
EOF
PYTHONPATH=src pytest test_greetlab.py -v
nano src/greetlab/cli.py
import argparse
import sys

def main():
    p = argparse.ArgumentParser()
    p.add_argument("--name", required=True)
    a = p.parse_args()
    # 新增：如果name去除首尾空白后为空，退出码2
    if not a.name.strip():
        sys.exit(2)
    print(f"Hello, {a.name}!")

if __name__ == "__main__":
    main()
PYTHONPATH=src pytest test_greetlab.py 
git diff
cat > ai_log.md <<'EOF'
为greetlab增加空白name校验，退出码2。
先编写测试用例，初次运行测试失败。
修改cli.py增加空白输入判断。
再次执行pytest，两个用例全部通过。
仅新增必要代码，无多余改动。
EOF

3.
cp -r q10 q11
cd q11
cat > communication.md <<'EOF'
## Issue
环境：Windows，Python环境版本待确认
复现命令：sdt-greet --name "   "
实际结果：输出 Hello, !，退出码0
期望结果：检测到纯空白name，以退出码2退出
描述：缺少空白姓名校验，纯空格参数仍正常执行。

## 提交信息
完善空白姓名参数校验
原程序对仅含空格的name不拦截，执行后正常输出问候，退出码0。新增空白判断，当name去除空格为空时，使用退出码2终止程序。

## 评审意见（Blocking）
行为：全空白name输入无拦截，程序正常执行。
风险：无法满足参数校验需求，测试用例失败。
建议：增加空白字符判断，纯空白输入返回退出码2，匹配测试预期。
EOF
4.
mkdir -p q12
cd q12
cat > train.py <<'EOF'
import torch
from torch import nn
torch.manual_seed(20260907)
x = torch.linspace(-1, 1, 101).reshape(-1, 1)
y = 3 * x - 1
model = nn.Linear(1, 1)
loss_fn = nn.MSELoss()
opt = torch.optim.SGD(model.parameters(), lr=0.1)

for _ in range(200):
    pred = model(x)
    loss = loss_fn(pred, y)
    opt.zero_grad()
    loss.backward()
    opt.step()

model.eval()
with torch.no_grad():
    pred_final = model(x)
    loss_final = loss_fn(pred_final, y)
    w = model.weight.item()
    b = model.bias.item()
    print(f"final loss: {loss_final:.6f}")
    print(f"weight: {w:.4f}")
    print(f"bias: {b:.4f}")
EOF
python3 train.py

实例
1.Save your environment with printenv to a file, create a venv, activate it,
 printenv to another file and diff before.txt after.txt. What changed in the 
 environment? Why does the shell prefer the venv? (Hint: look at $PATH before 
 and after activation.) Run which deactivate and reason about what the deactivate
bash function is doing.

激活虚拟环境后，PATH 开头追加了虚拟环境 bin 目录，新增 VIRTUAL_ENV 相关变量，
修改了命令行提示符 PS1。Shell 按照 PATH 从左到右寻找程序，虚拟环境目录排在最前面，
所以优先使用 venv 里的工具。deactivate 不是外部程序，是 bash 函数；它还原 PATH、
清除虚拟环境标记变量、恢复提示符，完成退出虚拟环境操作。
2.
Create a Python package with a pyproject.toml and install it in a virtual environment. Create a lockfile and inspect it.
mkdir q13 && cd q13
# 创建源码目录结构
mkdir -p src/demopkg
# 创建 __init__.py
echo > src/demopkg/__init__.py
# 写cli.py命令行入口
cat > src/demopkg/cli.py <<'EOF'
def main():
    print("Hello from demopkg!")
EOF
# 编写 pyproject.toml
cat > pyproject.toml <<'EOF'
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "demopkg"
version = "0.1.0"

[project.scripts]
demo-cli = "demopkg.cli:main"
EOF
python3 -m venv venv
source venv/bin/activate
demo-cli
pip install pip-tools
pip-compile pyproject.toml
cat requirements.txt
实验基于`pyproject.toml`创建 Python 本地软件包，在虚拟环境内完成包安装，并且生成、查看依赖锁文件。
3.
Install Docker and use it to build the Missing Semester class website locally using docker compose.
4.
用 AI 编程智能体浏览一个陌生的代码库。最好在调试或为你在意的项目添加新功能的背景下进行。
10.
Write a Dockerfile for a simple Python application. Then write a docker-compose.yml that runs your application alongside a Redis cache.
8.
浏览一个知名项目（例如 Redis 或 curl）的源码。 找出讲义中提到的几种注释类型：有用的 TODO、
指向外部文档的引用、解释为何避开某种做法的“why not”注释、或血泪教训。 如果这些注释不存在，会失去什么？
（1）有用的 TODO 注释
/* TODO: this would be a breaking change for scripts, do that in a major version. */
/* else if (r->type == REDIS_REPLY_PUSH) numsep = '>'; */
说明为什么现在不做（会破坏脚本兼容性）以及什么时候做。
（2）指向外部文档 / 资料的引用
/* Based on the following article (that apparently does not provide a
 * novel approach but only publicizes an already used technique):
 *
 * https://www.facebook.com/notes/facebook-engineering/three-optimization-tips-for-c/10151361643253920 */
`ull2string()` 这个整数转字符串的优化算法，作者把原始出处链接写在了函数头注释里。
（3）"Why not" 注释
/* Delete from the hash table and later from the skiplist.
 * Note that the order is important: deleting from the skiplist
 * actually releases the SDS string representing the element,
 * which is shared between the skiplist and the hash table, so
 * we need to delete from the skiplist as the final step. */
没有这段注释，后来人很可能觉得 "先删跳表再删哈希表" 或 "先删谁都行"，结果导致 use-after-free。
5
在你使用的所有支持 Vim mode 的软件中启用 Vim 模式，例如编辑器和 Shell，
并在接下来一个月里把所有文本编辑都用 Vim 模式完成。每当你觉得哪里低效，
或者想到“肯定有更好的办法”时，就去 Google 一下，通常真的有更好的办法。
6.
使用管道找出你「home 目录」中最常见的 5 种文件扩展名。（提示：组合 find 、grep / sed / awk、sort、uniq -c 以及 head）
7.
awk 可以按列值过滤行并改写输出。例如，awk '$3 ~ /pattern/ {$4=""; print}' 会只输出第三列匹配 pattern 的行，并省略第四列。请写一个 awk 命令：只输出第二列大于 100 的行，并交换第一列和第三列。可用这条命令测试：printf 'a 50 x\nb 150 y\nc 200 z\n'
9.
Publish a Python package to TestPyPI (don’t publish to the real PyPI unless it’s worth sharing!). Then build a Docker image with said package and push it to ghcr.io.


